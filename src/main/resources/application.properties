# Connection url for the database "db_golf"
spring.datasource.url = jdbc:mysql://localhost:3309/db_golf

# Username and password
spring.datasource.username = chakib
spring.datasource.password = scw4kGkivmqi8hwd

# Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in the project
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Configuration of server
server.port=8085
spring.thymeleaf.cache=true

# Allows us to configure error page
server.error.whitelabel.enabled=false
